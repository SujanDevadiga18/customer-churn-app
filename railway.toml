[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn backend.app:app --host 0.0.0.0 --port 8000"

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "pip install -r backend/requirements.txt"
]
